---
import {
  ArrowRight,
  BookOpen,
  Check,
  Cpu,
  Download,
  ExternalLink,
  Film,
  Folder,
  Gauge,
  Layers3,
  Music2,
  Play,
  Radio,
  ShieldCheck,
  Sliders,
  Sparkles,
  Subtitles,
  Sun,
  Terminal,
  Tv,
  Wifi,
  Zap,
  Moon,
} from "@lucide/astro";
import GitHubIcon from "../components/GitHubIcon.astro";

const releaseUrl = "https://github.com/Riteshp2001/mpvRx/releases/latest";
const repoUrl = "https://github.com/Riteshp2001/mpvRx";
const previewUrl = "https://riteshp2001.github.io/mpvRx/#preview";

const capabilities = [
  {
    icon: Gauge,
    tag: "Core Engine",
    title: "A playback engine you can tune",
    body: "libmpv at the core with hardware decoding, frame-stepping, A-B repeat, variable speed (0.5x–4.0x), refresh-rate auto-matching, and live diagnostics.",
    accent: "#8b6cff",
  },
  {
    icon: Layers3,
    tag: "Shaders & HDR",
    title: "HDR and shaders, under control",
    body: "77+ bundled shaders, HDR-toys tone-mapping pipelines, Anime4K upscaling tiers, GPU debanding, and thermal-aware safeguards for sustained performance.",
    accent: "#9b82ff",
  },
  {
    icon: Subtitles,
    tag: "Subtitles",
    title: "Subtitles without compromise",
    body: "Dual simultaneous tracks, ASS/SSA styling, Wyzie and SubtitleHub search, TMDB episode matching, custom font loading, and speech-to-subtitle tools.",
    accent: "#8b6cff",
  },
  {
    icon: Wifi,
    tag: "Network & Cloud",
    title: "Local, network, and online media",
    body: "Seamlessly browse device storage, SMB, FTP, SFTP, WebDAV, Jellyfin, Navidrome, direct HLS/DASH streams, M3U playlists, and torrent streaming.",
    accent: "#9b82ff",
  },
  {
    icon: Music2,
    tag: "Audio Suite",
    title: "A dedicated music experience",
    body: "Hi-Res FLAC/Opus playback, album artist grouping, synchronized LRC lyrics, cover art grid, persistent queue, and four real-time audio visualizers.",
    accent: "#8b6cff",
  },
  {
    icon: Terminal,
    tag: "Extensible",
    title: "Your player, fully scriptable",
    body: "In-app editing for mpv.conf, input.conf, Lua, and JavaScript. Register custom buttons and trigger native actions through the bidirectional bridge.",
    accent: "#bd9bff",
  },
];

const builds = [
  {
    name: "Standard",
    badge: "Recommended",
    badgeType: "primary",
    description: "Full Vulkan-capable release with dedicated arm64-v8a, armeabi-v7a, x86, and x86_64 builds plus a universal APK.",
    features: ["Vulkan & OpenGL ES backends", "Per-ABI optimized APKs", "Highest shader performance", "Regular automatic updates"],
    tag: "Best for 99% of Android devices",
  },
  {
    name: "No Vulkan",
    badge: "Compatibility",
    badgeType: "neutral",
    description: "Universal build designed for older hardware, budget chipsets, or custom ROMs with incomplete Vulkan driver support.",
    features: ["OpenGL ES rendering fallback", "Universal single APK", "Broadest device compatibility", "Reduced driver crash surface"],
    tag: "For older devices & custom ROMs",
  },
  {
    name: "FongMi",
    badge: "Alternate Native",
    badgeType: "accent",
    description: "Universal build featuring the alternate MediaCodec and Vulkan integration tuned for TV boxes and specific SoC decoders.",
    features: ["Alternate MediaCodec pipeline", "Universal single APK", "Optimized for Android TV & boxes", "Dedicated decoder path"],
    tag: "For Android TV & specialized SoCs",
  },
];

const showcaseGallery = [
  {
    image: "/images/showcase/player-controls.jpg",
    alt: "mpvRx touch-first playback controls",
    layout: "controls",
    orientation: "landscape",
    width: 2800,
    height: 1260,
  },
  {
    image: "/images/showcase/playlist-window.jpg",
    alt: "mpvRx visual playback queue",
    layout: "playlist",
    orientation: "landscape",
    width: 2800,
    height: 1260,
  },
  {
    image: "/images/showcase/chapter-navigation.jpg",
    alt: "mpvRx chapter navigation controls",
    layout: "chapters",
    orientation: "landscape",
    width: 2800,
    height: 1260,
  },
  {
    image: "/images/showcase/pip.png",
    alt: "mpvRx picture-in-picture mode",
    layout: "pip",
    orientation: "portrait",
    width: 720,
    height: 1600,
  },
  {
    image: "/images/showcase/music-player.jpg",
    alt: "mpvRx music player",
    layout: "music",
    orientation: "portrait",
    width: 1080,
    height: 2400,
  },
  {
    image: "/images/showcase/about-screen.jpg",
    alt: "mpvRx About screen",
    layout: "about",
    orientation: "portrait",
    width: 1260,
    height: 2800,
  },
];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="description"
      content="mpvRx is a focused Android media player powered by mpv, with expressive touch controls, HDR tone mapping, Anime4K shaders, network streaming, and Lua/JS scripting."
    />
    <meta name="theme-color" content="#0c0a14" />
    <meta property="og:title" content="mpvRx - Android media, without the noise" />
    <meta property="og:description" content="libmpv precision, touch-first gesture controls, HDR shaders, network streaming, and open-source freedom on Android." />
    <meta property="og:image" content="/images/showcase/tanjiro-playback.jpg" />
    <title>mpvRx | Android media, without the noise</title>
    <link rel="icon" type="image/png" href="/images/icon.png" />
    <script is:inline>
      (() => {
        try {
          const savedTheme = localStorage.getItem("mpvrx-theme");
          const systemTheme = matchMedia("(prefers-color-scheme: light)").matches ? "light" : "dark";
          document.documentElement.dataset.theme = savedTheme === "light" || savedTheme === "dark" ? savedTheme : systemTheme;
        } catch {
          document.documentElement.dataset.theme = "dark";
        }
      })();
    </script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@400;500;600;700&family=Space+Grotesk:wght@500;600;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <!-- Site Header -->
    <header class="site-header">
      <div class="header-inner">
        <a class="brand" href="/" aria-label="mpvRx home">
          <div class="brand-icon-wrap">
            <img src="/images/icon.png" alt="mpvRx icon" width="34" height="34" class="brand-img" />
          </div>
          <span class="brand-text">mpvRx</span>
          <span class="version-badge">v2.5.0</span>
        </a>

        <nav class="nav-links" aria-label="Primary navigation">
          <a href="#features">Features</a>
          <a href="#showcase">Showcase</a>
          <a href="#builds">Builds</a>
          <a href="/docs">Docs</a>
        </nav>

        <div class="header-actions">
          <button class="theme-toggle" type="button" data-theme-toggle aria-label="Switch color theme" title="Switch color theme">
            <Sun class="theme-icon theme-icon-sun" size={18} strokeWidth={1.9} />
            <Moon class="theme-icon theme-icon-moon" size={18} strokeWidth={1.9} />
          </button>
          <a class="nav-icon-btn" href={repoUrl} target="_blank" rel="noopener noreferrer" aria-label="mpvRx on GitHub">
            <GitHubIcon size={18} />
            <span class="github-label">GitHub</span>
          </a>
          <a class="header-cta" href={releaseUrl} target="_blank" rel="noopener noreferrer">
            <Download size={16} strokeWidth={2.2} />
            <span>Download</span>
          </a>
        </div>
      </div>
    </header>

    <main>
      <!-- Hero Section -->
      <section class="hero-section" aria-labelledby="hero-title">
        <div class="hero-glow"></div>
        <div class="hero-glow-secondary"></div>

        <div class="container hero-container">
          <div class="hero-content">
            <div class="hero-pill-badge">
              <span class="pulse-dot"></span>
              <span>Open Source · Android 8.0+ · libmpv Core</span>
            </div>

            <h1 id="hero-title" class="hero-headline">
              Android media,<br />
              <span class="gradient-text">without the noise.</span>
            </h1>

            <p class="hero-description">
              The precision of <code class="inline-code">mpv</code> paired with a modern touch-first interface.
              Zero ads, no analytics tracking, deep playback controls, 77+ bundled shaders, and full scripting freedom.
            </p>

            <div class="hero-btn-row">
              <a class="btn btn-primary" href={releaseUrl} target="_blank" rel="noopener noreferrer">
                <Download size={19} strokeWidth={2.2} />
                <span>Download Latest APK</span>
              </a>
              <a class="btn btn-secondary" href="/docs">
                <BookOpen size={19} strokeWidth={2} />
                <span>Documentation</span>
              </a>
              <a class="btn btn-ghost" href={repoUrl} target="_blank" rel="noopener noreferrer">
                <GitHubIcon size={18} />
                <span>Source Code</span>
              </a>
            </div>

            <div class="hero-tags-row">
              <span class="feature-tag"><Check size={14} /> Hardware Accelerated</span>
              <span class="feature-tag"><Check size={14} /> HDR Tone Mapping</span>
              <span class="feature-tag"><Check size={14} /> Dual Subtitles</span>
              <span class="feature-tag"><Check size={14} /> 100% Free & Open Source</span>
            </div>
          </div>

          <!-- Hero Media Showcase Mockup -->
          <div class="hero-media-wrapper">
            <div class="mockup-frame">
              <div class="mockup-bar">
                <div class="mockup-dots">
                  <span></span><span></span><span></span>
                </div>
                <span class="mockup-title">mpvRx Player View</span>
                <span class="mockup-chip">4K HDR · 60fps</span>
              </div>
              <div class="mockup-image-container">
                <img
                  src="/images/showcase/tanjiro-playback.jpg"
                  alt="Video playback in mpvRx"
                  class="mockup-image"
                  width="1920"
                  height="1080"
                />
                <div class="mockup-overlay-badge left-badge">
                  <Sliders size={15} />
                  <span>Hold-for-Speed & Gesture Engine</span>
                </div>
                <div class="mockup-overlay-badge right-badge">
                  <Sparkles size={15} />
                  <span>Anime4K & HDR-toys Active</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Signal Stat Strip -->
      <section class="stat-strip" aria-label="mpvRx key metrics">
        <div class="container stat-grid">
          <div class="stat-item">
            <div class="stat-icon-wrap"><Layers3 size={20} /></div>
            <div class="stat-data">
              <strong>77+</strong>
              <span>Bundled Shaders & Tone Maps</span>
            </div>
          </div>
          <div class="stat-item">
            <div class="stat-icon-wrap"><Cpu size={20} /></div>
            <div class="stat-data">
              <strong>3</strong>
              <span>Native Build Flavors</span>
            </div>
          </div>
          <div class="stat-item">
            <div class="stat-icon-wrap"><Wifi size={20} /></div>
            <div class="stat-data">
              <strong>4</strong>
              <span>Network Share Protocols</span>
            </div>
          </div>
          <div class="stat-item">
            <div class="stat-icon-wrap"><ShieldCheck size={20} /></div>
            <div class="stat-data">
              <strong>0</strong>
              <span>Trackers, Ads, or Telemetry</span>
            </div>
          </div>
        </div>
      </section>

      <!-- Capabilities / Bento Feature Grid -->
      <section class="section features-section" id="features" aria-labelledby="features-title">
        <div class="container">
          <div class="section-header">
            <p class="section-eyebrow">
              <span class="eyebrow-line"></span> Deep Engine Capabilities
            </p>
            <h2 id="features-title" class="section-title">
              Power where it matters.<br />
              <span class="gradient-text">Quiet everywhere else.</span>
            </h2>
            <p class="section-subtitle">
              mpvRx keeps video content front and center. Advanced diagnostics, post-processing shaders,
              and subtitle controls appear instantly when requested and disappear when you watch.
            </p>
          </div>

          <div class="bento-grid">
            {capabilities.map(({ icon: Icon, tag, title, body, accent }, index) => (
              <article class="bento-card" style={`--accent-color:${accent}; --index:${index}`}>
                <div class="card-glow"></div>
                <div class="card-top">
                  <div class="card-icon-box">
                    <Icon size={22} strokeWidth={2} />
                  </div>
                  <span class="card-tag">{tag}</span>
                </div>
                <h3 class="card-title">{title}</h3>
                <p class="card-body">{body}</p>
              </article>
            ))}
          </div>
        </div>
      </section>

      <!-- App Screenshot Gallery Showcase -->
      <section class="section showcase-section" id="showcase" aria-labelledby="showcase-title">
        <div class="container">
          <div class="section-header">
            <p class="section-eyebrow">
              <span class="eyebrow-line"></span> Complete Media Surface
            </p>
            <h2 id="showcase-title" class="section-title">
              From your file system<br />
              <span class="gradient-text">to the exact target frame.</span>
            </h2>
            <p class="section-subtitle">
              Every screen is engineered for fast navigation, intuitive touch gestures, and deep media control.
            </p>
          </div>

          <div class="showcase-bento" aria-label="mpvRx interface screenshots">
            {showcaseGallery.map(({ image, alt, layout, orientation, width, height }) => (
              <figure class:list={["showcase-tile", `showcase-tile--${layout}`, `showcase-tile--${orientation}`]}>
                <img src={image} alt={alt} loading="lazy" width={width} height={height} />
              </figure>
            ))}
          </div>
        </div>
      </section>

      <!-- Principles & Philosophy (Built in the Open) -->
      <section class="section philosophy-section" aria-labelledby="philosophy-title">
        <div class="container">
          <div class="philosophy-card">
            <div class="philosophy-grid">
              <div class="philosophy-main">
                <div class="philosophy-badge">
                  <ShieldCheck size={20} />
                  <span>AGPL-3.0 Licensed Open Source</span>
                </div>
                <h2 id="philosophy-title" class="philosophy-headline">
                  Your files are never the product.
                </h2>
                <p class="philosophy-text">
                  mpvRx is fully open source. It publishes every line of code, complete commit histories,
                  and verifiable build variants on GitHub. No analytics SDKs, no ad banners, no subscription gates.
                </p>
                <div class="philosophy-links">
                  <a class="link-btn" href="/docs/reference/privacy-and-permissions">
                    <span>Privacy & Permissions Guide</span>
                    <ArrowRight size={16} />
                  </a>
                  <a class="link-btn-subtle" href={repoUrl} target="_blank" rel="noopener noreferrer">
                    <span>Inspect Source on GitHub</span>
                    <ExternalLink size={15} />
                  </a>
                </div>
              </div>

              <div class="philosophy-pillars">
                <div class="pillar-box">
                  <span class="pillar-num">01</span>
                  <div>
                    <h4>Local First Architecture</h4>
                    <p>Your local device storage and offline media are always the primary citizen.</p>
                  </div>
                </div>
                <div class="pillar-box">
                  <span class="pillar-num">02</span>
                  <div>
                    <h4>Explicit Network Access</h4>
                    <p>Connections to SMB, SFTP, WebDAV, or Jellyfin only open when you explicitly initiate them.</p>
                  </div>
                </div>
                <div class="pillar-box">
                  <span class="pillar-num">03</span>
                  <div>
                    <h4>Transparent Builds</h4>
                    <p>Public source, release workflows, and checksummed artifacts keep every distribution path inspectable.</p>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Native Build Variants Section -->
      <section class="section builds-section" id="builds" aria-labelledby="builds-title">
        <div class="container">
          <div class="section-header">
            <p class="section-eyebrow">
              <span class="eyebrow-line"></span> Targeted Binaries
            </p>
            <h2 id="builds-title" class="section-title">
              One player.<br />
              <span class="gradient-text">Three native build variants.</span>
            </h2>
            <p class="section-subtitle">
              Start with Standard for modern devices. Choose specialized variants when your chipset or display setup requires a specific decoding stack.
            </p>
          </div>

          <div class="builds-grid">
            {builds.map((build, index) => (
              <article class:list={["build-card", { "build-featured": index === 0 }]}>
                {index === 0 && <div class="featured-ribbon">Most Popular</div>}
                <div class="build-card-header">
                  <div>
                    <span class="build-variant-index">Flavor 0{index + 1}</span>
                    <h3 class="build-name">{build.name}</h3>
                  </div>
                  <span class:list={["build-chip", `chip-${build.badgeType}`]}>{build.badge}</span>
                </div>

                <p class="build-desc">{build.description}</p>

                <div class="build-feature-list">
                  {build.features.map((feat) => (
                    <div class="build-feature-row">
                      <Check size={16} class="check-icon" />
                      <span>{feat}</span>
                    </div>
                  ))}
                </div>

                <div class="build-footer">
                  <span class="build-tag-text">{build.tag}</span>
                  <a class="build-link-btn" href="/docs/getting-started/build-variants">
                    <span>Compare specs</span>
                    <ArrowRight size={15} />
                  </a>
                </div>
              </article>
            ))}
          </div>
        </div>
      </section>

      <!-- Final Call to Action -->
      <section class="final-cta-section">
        <div class="final-cta-bg">
          <div class="final-cta-overlay"></div>
        </div>

        <div class="container final-cta-container">
          <div class="final-cta-icon-box">
            <Play size={28} fill="currentColor" />
          </div>
          <h2 class="final-cta-title">Make playback truly yours.</h2>
          <p class="final-cta-subtitle">
            Get mpvRx on your Android phone, tablet, or TV. Built for enthusiasts who care about video quality, audio precision, and uncompromising control.
          </p>

          <div class="final-cta-actions">
            <a class="btn btn-primary btn-large" href={releaseUrl} target="_blank" rel="noopener noreferrer">
              <Download size={20} strokeWidth={2.2} />
              <span>Download mpvRx APK</span>
            </a>
            <a class="btn btn-secondary btn-large" href="/docs/getting-started/first-playback">
              <BookOpen size={20} strokeWidth={2} />
              <span>Getting Started Guide</span>
            </a>
          </div>
        </div>
      </section>

      <section class="preview-channel" aria-labelledby="preview-title">
        <div class="container preview-channel-inner">
          <div class="preview-copy">
            <p class="preview-kicker"><Radio size={17} /> Preview channel</p>
            <h2 id="preview-title">Try the next mpvRx build early.</h2>
            <p>
              Preview builds are signed and published from the latest selected source for testers who want new fixes and features before the next stable release.
            </p>
            <div class="preview-tags" aria-label="Available preview variants">
              <span>Standard</span>
              <span>FongMi</span>
              <span>No Vulkan</span>
              <span>Checksummed</span>
            </div>
          </div>
          <div class="preview-actions">
            <a class="btn btn-primary btn-large" href={previewUrl} target="_blank" rel="noopener noreferrer">
              <Radio size={20} />
              <span>Open preview builds</span>
              <ExternalLink size={16} />
            </a>
            <p>Preview builds may contain regressions. Keep the stable APK available and include the preview revision in issue reports.</p>
          </div>
        </div>
      </section>
    </main>

    <!-- Site Footer -->
    <footer class="site-footer">
      <div class="container footer-container">
        <div class="footer-brand-col">
          <a class="brand" href="/" aria-label="mpvRx home">
            <img src="/images/icon.png" alt="mpvRx logo" width="30" height="30" class="brand-img" />
            <span class="brand-text">mpvRx</span>
          </a>
          <p class="footer-tagline">
            Focused Android media playback powered by libmpv. Open source under AGPL-3.0-or-later.
          </p>
        </div>

        <div class="footer-links-grid">
          <div class="footer-link-group">
            <h5>Product</h5>
            <a href="#features">Features</a>
            <a href="#showcase">Screenshots</a>
            <a href="#builds">Build Variants</a>
            <a href={releaseUrl} target="_blank" rel="noopener noreferrer">Releases</a>
            <a href={previewUrl} target="_blank" rel="noopener noreferrer">Preview Builds</a>
          </div>
          <div class="footer-link-group">
            <h5>Documentation</h5>
            <a href="/docs/getting-started/installation">Installation</a>
            <a href="/docs/playback/controls-and-gestures">Gestures & Controls</a>
            <a href="/docs/playback/video-hdr-and-shaders">Shaders & HDR</a>
            <a href="/docs/customization/custom-commands">Scripting Bridge</a>
          </div>
          <div class="footer-link-group">
            <h5>Community</h5>
            <a href={repoUrl} target="_blank" rel="noopener noreferrer">GitHub Repository</a>
            <a href={`${repoUrl}/issues`} target="_blank" rel="noopener noreferrer">Issue Tracker</a>
            <a href="/docs/reference/support-and-contributing">Contributing</a>
            <a href="/docs/reference/privacy-and-permissions">Privacy Notice</a>
          </div>
        </div>
      </div>

      <div class="container footer-bottom">
        <p>© {new Date().getFullYear()} mpvRx contributors. Free software under AGPL-3.0.</p>
        <div class="footer-bottom-links">
          <a href={repoUrl} target="_blank" rel="noopener noreferrer">GitHub</a>
          <span class="footer-dot">·</span>
          <a href="/docs">Docs</a>
          <span class="footer-dot">·</span>
          <a href="https://mpv.io" target="_blank" rel="noopener noreferrer">mpv.io</a>
        </div>
      </div>
    </footer>
    <script>
      const root = document.documentElement;
      const toggle = document.querySelector("[data-theme-toggle]");
      const themeColor = document.querySelector('meta[name="theme-color"]');

      const updateThemeControl = (theme) => {
        const nextTheme = theme === "dark" ? "light" : "dark";
        toggle?.setAttribute("aria-label", `Switch to ${nextTheme} theme`);
        toggle?.setAttribute("title", `Switch to ${nextTheme} theme`);
        themeColor?.setAttribute("content", theme === "dark" ? "#0c0a14" : "#f7f5ff");
      };

      const applyTheme = (theme) => {
        root.dataset.theme = theme;
        localStorage.setItem("mpvrx-theme", theme);
        updateThemeControl(theme);
      };

      updateThemeControl(root.dataset.theme === "light" ? "light" : "dark");

      toggle?.addEventListener("click", async (event) => {
        const nextTheme = root.dataset.theme === "light" ? "dark" : "light";
        const reduceMotion = matchMedia("(prefers-reduced-motion: reduce)").matches;

        if (!("startViewTransition" in document) || reduceMotion) {
          applyTheme(nextTheme);
          return;
        }

        const bounds = toggle.getBoundingClientRect();
        const pointerEvent = event;
        const x = pointerEvent.clientX || bounds.left + bounds.width / 2;
        const y = pointerEvent.clientY || bounds.top + bounds.height / 2;
        const radius = Math.hypot(Math.max(x, innerWidth - x), Math.max(y, innerHeight - y));
        const transition = document.startViewTransition(() => applyTheme(nextTheme));

        await transition.ready;
        root.animate(
          {
            clipPath: [`circle(0 at ${x}px ${y}px)`, `circle(${radius}px at ${x}px ${y}px)`],
            filter: ["blur(18px)", "blur(0)"],
          },
          {
            duration: 650,
            easing: "cubic-bezier(0.22, 1, 0.36, 1)",
            pseudoElement: "::view-transition-new(root)",
          },
        );
      });
    </script>
  </body>
</html>

<style is:global>
  :root {
    --bg-base: #0c0a14;
    --bg-surface: #151121;
    --bg-surface-elevated: #1c162b;
    --bg-surface-hover: #261e38;
    --border-subtle: rgba(255, 255, 255, 0.08);
    --border-medium: rgba(255, 255, 255, 0.14);
    --border-hover: rgba(139, 108, 255, 0.48);
    --text-primary: #f7f4ff;
    --text-secondary: #c1b9d3;
    --text-muted: #8d849f;
    --accent-teal: #8b6cff;
    --accent-teal-bright: #9b82ff;
    --accent-teal-soft: rgba(139, 108, 255, 0.14);
    --accent-orange: #bd9bff;
    --accent-orange-soft: rgba(189, 155, 255, 0.16);
    color-scheme: dark;
    font-family: "IBM Plex Sans", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
    background: var(--bg-base);
    color: var(--text-primary);
  }

  :root[data-theme="light"] {
    --bg-base: #f7f5ff;
    --bg-surface: #ffffff;
    --bg-surface-elevated: #f0ecff;
    --bg-surface-hover: #e8e1ff;
    --border-subtle: rgba(54, 38, 92, 0.12);
    --border-medium: rgba(54, 38, 92, 0.2);
    --border-hover: rgba(101, 71, 215, 0.46);
    --text-primary: #181225;
    --text-secondary: #4f465f;
    --text-muted: #756b85;
    --accent-teal: #7456df;
    --accent-teal-bright: #6547d7;
    --accent-teal-soft: rgba(101, 71, 215, 0.1);
    --accent-orange: #8b6cff;
    --accent-orange-soft: rgba(139, 108, 255, 0.12);
    color-scheme: light;
  }

  ::view-transition-old(root),
  ::view-transition-new(root) {
    animation: none;
    mix-blend-mode: normal;
  }

  ::view-transition-old(root) {
    z-index: 1;
  }

  ::view-transition-new(root) {
    z-index: 9999;
  }

  * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  html {
    scroll-behavior: smooth;
  }

  body {
    background-color: var(--bg-base);
    color: var(--text-primary);
    line-height: 1.6;
    overflow-x: hidden;
    -webkit-font-smoothing: antialiased;
  }

  a {
    color: inherit;
    text-decoration: none;
    transition: all 0.2s ease;
  }

  img {
    display: block;
    max-width: 100%;
    height: auto;
  }

  .container {
    width: 100%;
    max-width: 1240px;
    margin: 0 auto;
    padding: 0 24px;
  }

  /* Typography Utilities */
  h1, h2, h3, h4, h5, h6 {
    font-family: "Space Grotesk", sans-serif;
    letter-spacing: -0.02em;
    font-weight: 700;
  }

  .gradient-text {
    background: linear-gradient(135deg, #a78bfa 0%, #7657e8 55%, #c4b5fd 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .inline-code {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.9em;
    padding: 2px 7px;
    border-radius: 4px;
    background: rgba(255, 255, 255, 0.08);
    color: var(--accent-teal-bright);
    border: 1px solid var(--border-subtle);
  }

  /* Site Header */
  .site-header {
    position: sticky;
    top: 0;
    z-index: 100;
    width: 100%;
    background: rgba(12, 10, 20, 0.84);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid var(--border-subtle);
  }

  .header-inner {
    max-width: 1240px;
    margin: 0 auto;
    padding: 14px 24px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .brand {
    display: inline-flex;
    align-items: center;
    gap: 10px;
  }

  .brand-icon-wrap {
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 8px;
    overflow: hidden;
    box-shadow: 0 0 18px rgba(139, 108, 255, 0.3);
  }

  .brand-img {
    border-radius: 8px;
  }

  .brand-text {
    font-family: "Space Grotesk", sans-serif;
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--text-primary);
  }

  .version-badge {
    padding: 2px 8px;
    border-radius: 12px;
    font-size: 0.72rem;
    font-weight: 600;
    background: var(--accent-teal-soft);
    color: var(--accent-teal-bright);
    border: 1px solid rgba(139, 108, 255, 0.28);
  }

  .nav-links {
    display: flex;
    align-items: center;
    gap: 28px;
    font-size: 0.92rem;
    font-weight: 500;
    color: var(--text-secondary);
  }

  .nav-links a:hover {
    color: var(--accent-teal-bright);
  }

  .header-actions {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .theme-toggle {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 38px;
    height: 38px;
    padding: 0;
    border: 1px solid var(--border-subtle);
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.04);
    color: var(--text-secondary);
    cursor: pointer;
    transition: color 0.2s ease, border-color 0.2s ease, background 0.2s ease, transform 0.2s ease;
  }

  .theme-toggle:hover {
    color: var(--accent-teal-bright);
    border-color: var(--border-hover);
    background: var(--accent-teal-soft);
    transform: translateY(-1px);
  }

  .theme-icon-sun {
    display: none;
  }

  :root[data-theme="dark"] .theme-icon-sun {
    display: block;
  }

  :root[data-theme="dark"] .theme-icon-moon {
    display: none;
  }

  .nav-icon-btn {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 7px 14px;
    border-radius: 6px;
    font-size: 0.88rem;
    font-weight: 600;
    color: var(--text-secondary);
    border: 1px solid var(--border-subtle);
    background: rgba(255, 255, 255, 0.03);
  }

  .nav-icon-btn:hover {
    color: var(--text-primary);
    border-color: var(--border-medium);
    background: rgba(255, 255, 255, 0.07);
  }

  .header-cta {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    padding: 7px 16px;
    border-radius: 6px;
    font-size: 0.88rem;
    font-weight: 700;
    background: var(--accent-teal-bright);
    color: #0c0e0f;
  }

  .header-cta:hover {
    background: #b6a2ff;
    transform: translateY(-1px);
    box-shadow: 0 4px 18px rgba(139, 108, 255, 0.36);
  }

  /* Buttons */
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 9px;
    min-height: 48px;
    padding: 0 22px;
    border-radius: 8px;
    font-size: 0.95rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .btn-large {
    min-height: 52px;
    padding: 0 28px;
    font-size: 1.02rem;
  }

  .btn-primary {
    background: var(--accent-orange);
    color: #0c0e0f;
    border: 1px solid rgba(189, 155, 255, 0.82);
    box-shadow: 0 4px 20px rgba(139, 108, 255, 0.34);
  }

  .btn-primary:hover {
    background: #cbb9ff;
    transform: translateY(-2px);
    box-shadow: 0 6px 26px rgba(139, 108, 255, 0.48);
  }

  .btn-secondary {
    background: rgba(255, 255, 255, 0.06);
    color: var(--text-primary);
    border: 1px solid var(--border-medium);
    backdrop-filter: blur(12px);
  }

  .btn-secondary:hover {
    background: rgba(255, 255, 255, 0.12);
    border-color: rgba(255, 255, 255, 0.25);
    transform: translateY(-2px);
  }

  .btn-ghost {
    background: transparent;
    color: var(--text-secondary);
    border: 1px solid var(--border-subtle);
  }

  .btn-ghost:hover {
    color: var(--text-primary);
    border-color: var(--border-medium);
    background: rgba(255, 255, 255, 0.04);
  }

  /* Hero Section */
  .hero-section {
    position: relative;
    padding: 60px 0 80px;
    overflow: hidden;
  }

  .hero-glow {
    position: absolute;
    top: -120px;
    left: 50%;
    transform: translateX(-50%);
    width: 900px;
    height: 480px;
    background: radial-gradient(ellipse at center, rgba(139, 108, 255, 0.22) 0%, rgba(139, 108, 255, 0) 70%);
    pointer-events: none;
    z-index: 0;
  }

  .hero-glow-secondary {
    position: absolute;
    top: 200px;
    right: -100px;
    width: 600px;
    height: 600px;
    background: radial-gradient(ellipse at center, rgba(189, 155, 255, 0.12) 0%, transparent 70%);
    pointer-events: none;
    z-index: 0;
  }

  .hero-container {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .hero-content {
    max-width: 820px;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .hero-pill-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 6px 16px;
    border-radius: 100px;
    background: rgba(139, 108, 255, 0.1);
    border: 1px solid rgba(139, 108, 255, 0.28);
    color: var(--accent-teal-bright);
    font-size: 0.82rem;
    font-weight: 600;
    letter-spacing: 0.02em;
    margin-bottom: 24px;
  }

  .pulse-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--accent-teal-bright);
    box-shadow: 0 0 10px var(--accent-teal-bright);
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%, 100% { transform: scale(1); opacity: 1; }
    50% { transform: scale(1.3); opacity: 0.7; }
  }

  .hero-headline {
    font-size: clamp(2.8rem, 7vw, 5.2rem);
    line-height: 1.04;
    color: var(--text-primary);
    margin-bottom: 20px;
  }

  .hero-description {
    font-size: clamp(1.05rem, 2vw, 1.25rem);
    color: var(--text-secondary);
    line-height: 1.65;
    max-width: 680px;
    margin-bottom: 36px;
  }

  .hero-btn-row {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    gap: 14px;
    margin-bottom: 30px;
  }

  .hero-tags-row {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 16px;
    color: var(--text-muted);
    font-size: 0.86rem;
    font-weight: 500;
  }

  .feature-tag {
    display: inline-flex;
    align-items: center;
    gap: 6px;
  }

  .feature-tag svg {
    color: var(--accent-teal-bright);
  }

  /* Mockup Frame */
  .hero-media-wrapper {
    margin-top: 56px;
    width: 100%;
    max-width: 1060px;
  }

  .mockup-frame {
    border-radius: 16px;
    background: var(--bg-surface);
    border: 1px solid var(--border-medium);
    box-shadow: 0 24px 64px -12px rgba(0, 0, 0, 0.85), 0 0 42px rgba(139, 108, 255, 0.18);
    overflow: hidden;
    transition: transform 0.3s ease, border-color 0.3s ease;
  }

  .mockup-frame:hover {
    border-color: rgba(139, 108, 255, 0.42);
  }

  .mockup-bar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 10px 18px;
    background: rgba(12, 14, 15, 0.65);
    border-bottom: 1px solid var(--border-subtle);
  }

  .mockup-dots {
    display: flex;
    gap: 6px;
  }

  .mockup-dots span {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.16);
  }

  .mockup-dots span:first-child { background: #ff5f56; }
  .mockup-dots span:nth-child(2) { background: #ffbd2e; }
  .mockup-dots span:nth-child(3) { background: #27c93f; }

  .mockup-title {
    font-size: 0.78rem;
    color: var(--text-muted);
    font-weight: 500;
  }

  .mockup-chip {
    padding: 2px 8px;
    border-radius: 4px;
    font-size: 0.72rem;
    font-weight: 600;
    background: rgba(139, 108, 255, 0.12);
    color: var(--accent-teal-bright);
    border: 1px solid rgba(139, 108, 255, 0.24);
  }

  .mockup-image-container {
    position: relative;
    background: #000;
    overflow: hidden;
  }

  .mockup-image {
    width: 100%;
    height: auto;
    object-fit: cover;
  }

  .mockup-overlay-badge {
    position: absolute;
    bottom: 20px;
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 16px;
    border-radius: 100px;
    background: rgba(12, 14, 15, 0.85);
    backdrop-filter: blur(14px);
    border: 1px solid var(--border-medium);
    font-size: 0.82rem;
    font-weight: 600;
    color: #f7f4ff;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.6);
  }

  .mockup-overlay-badge svg {
    color: #cbbdff;
  }

  .left-badge { left: 24px; }
  .right-badge { right: 24px; }

  /* Stat Strip */
  .stat-strip {
    border-top: 1px solid var(--border-subtle);
    border-bottom: 1px solid var(--border-subtle);
    background: var(--bg-surface);
    padding: 28px 0;
  }

  .stat-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 24px;
  }

  .stat-item {
    display: flex;
    align-items: center;
    gap: 16px;
    padding: 8px 12px;
  }

  .stat-icon-wrap {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 44px;
    height: 44px;
    border-radius: 10px;
    background: var(--accent-teal-soft);
    color: var(--accent-teal-bright);
    border: 1px solid rgba(139, 108, 255, 0.24);
    flex-shrink: 0;
  }

  .stat-data strong {
    display: block;
    font-family: "Space Grotesk", sans-serif;
    font-size: 1.7rem;
    line-height: 1.1;
    color: var(--text-primary);
  }

  .stat-data span {
    font-size: 0.82rem;
    color: var(--text-muted);
  }

  /* Section Common */
  .section {
    padding: 100px 0;
    position: relative;
  }

  .section-header {
    max-width: 760px;
    margin-bottom: 56px;
  }

  .section-eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    font-size: 0.8rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--accent-teal-bright);
    margin-bottom: 14px;
  }

  .eyebrow-line {
    width: 22px;
    height: 2px;
    background: var(--accent-teal-bright);
  }

  .section-title {
    font-size: clamp(2.2rem, 5vw, 3.6rem);
    line-height: 1.08;
    color: var(--text-primary);
    margin-bottom: 16px;
  }

  .section-subtitle {
    font-size: 1.08rem;
    color: var(--text-secondary);
    line-height: 1.6;
  }

  /* Bento Grid */
  .bento-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
  }

  .bento-card {
    position: relative;
    border-radius: 14px;
    background: var(--bg-surface);
    border: 1px solid var(--border-subtle);
    padding: 32px 28px;
    display: flex;
    flex-direction: column;
    overflow: hidden;
    transition: all 0.25s ease;
  }

  .bento-card:hover {
    transform: translateY(-4px);
    border-color: var(--border-hover);
    background: var(--bg-surface-elevated);
    box-shadow: 0 16px 36px -8px rgba(0, 0, 0, 0.6);
  }

  .card-glow {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: linear-gradient(90deg, transparent, var(--accent-color), transparent);
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .bento-card:hover .card-glow {
    opacity: 1;
  }

  .card-top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 24px;
  }

  .card-icon-box {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 44px;
    height: 44px;
    border-radius: 10px;
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid var(--border-medium);
    color: var(--accent-teal-bright);
  }

  .card-tag {
    font-size: 0.72rem;
    font-weight: 700;
    text-transform: uppercase;
    padding: 3px 10px;
    border-radius: 100px;
    background: rgba(255, 255, 255, 0.04);
    color: var(--text-muted);
    border: 1px solid var(--border-subtle);
  }

  .card-title {
    font-size: 1.25rem;
    color: var(--text-primary);
    margin-bottom: 12px;
    line-height: 1.25;
  }

  .card-body {
    font-size: 0.94rem;
    color: var(--text-secondary);
    line-height: 1.6;
  }

  /* Screenshot Showcase */
  .showcase-bento {
    display: grid;
    grid-template-columns: repeat(12, minmax(0, 1fr));
    grid-auto-flow: dense;
    grid-auto-rows: 72px;
    gap: 18px;
  }

  .showcase-tile {
    position: relative;
    min-width: 0;
    overflow: hidden;
    border: 1px solid var(--border-subtle);
    border-radius: 14px;
    background: #09070e;
    box-shadow: 0 18px 44px -18px rgba(0, 0, 0, 0.72);
    transition: border-color 0.25s ease, box-shadow 0.25s ease;
  }

  .showcase-tile:hover {
    border-color: var(--border-hover);
    box-shadow: 0 22px 48px -16px rgba(0, 0, 0, 0.82);
  }

  .showcase-tile img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: contain;
    transition: transform 0.35s ease;
  }

  .showcase-tile:hover img {
    transform: scale(1.012);
  }

  .showcase-tile--controls {
    grid-column: span 8;
    grid-row: span 4;
  }

  .showcase-tile--playlist,
  .showcase-tile--chapters {
    grid-column: span 4;
    grid-row: span 2;
  }

  .showcase-tile--portrait {
    grid-column: span 4;
    grid-row: span 7;
    background:
      radial-gradient(circle at 50% 38%, rgba(139, 108, 255, 0.12), transparent 44%),
      #09070e;
  }

  /* Philosophy & Privacy */
  .philosophy-card {
    border-radius: 20px;
    background: linear-gradient(135deg, rgba(20, 23, 25, 0.9) 0%, rgba(12, 14, 15, 0.95) 100%);
    border: 1px solid var(--border-medium);
    padding: clamp(36px, 6vw, 64px);
    box-shadow: 0 24px 64px rgba(0, 0, 0, 0.6);
  }

  .philosophy-grid {
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    gap: 48px;
    align-items: center;
  }

  .philosophy-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 6px 14px;
    border-radius: 100px;
    background: var(--accent-teal-soft);
    color: var(--accent-teal-bright);
    font-size: 0.8rem;
    font-weight: 600;
    border: 1px solid rgba(139, 108, 255, 0.28);
    margin-bottom: 20px;
  }

  .philosophy-headline {
    font-size: clamp(2.2rem, 4.5vw, 3.4rem);
    line-height: 1.08;
    color: var(--text-primary);
    margin-bottom: 18px;
  }

  .philosophy-text {
    font-size: 1.05rem;
    color: var(--text-secondary);
    line-height: 1.65;
    margin-bottom: 28px;
  }

  .philosophy-links {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 16px;
  }

  .link-btn {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 10px 18px;
    border-radius: 6px;
    background: var(--accent-teal-bright);
    color: #0c0e0f;
    font-weight: 700;
    font-size: 0.92rem;
  }

  .link-btn:hover {
    background: #b6a2ff;
    transform: translateY(-1px);
  }

  .link-btn-subtle {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    padding: 10px 16px;
    color: var(--text-secondary);
    font-weight: 600;
    font-size: 0.92rem;
  }

  .link-btn-subtle:hover {
    color: var(--text-primary);
  }

  .philosophy-pillars {
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .pillar-box {
    display: flex;
    gap: 18px;
    padding: 20px;
    border-radius: 12px;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid var(--border-subtle);
    transition: all 0.2s ease;
  }

  .pillar-box:hover {
    background: rgba(255, 255, 255, 0.06);
    border-color: var(--border-medium);
  }

  .pillar-num {
    font-family: "Space Grotesk", sans-serif;
    font-weight: 700;
    font-size: 1.2rem;
    color: var(--accent-teal-bright);
  }

  .pillar-box h4 {
    font-size: 1.05rem;
    color: var(--text-primary);
    margin-bottom: 4px;
  }

  .pillar-box p {
    font-size: 0.88rem;
    color: var(--text-secondary);
    line-height: 1.5;
  }

  /* Builds Section */
  .builds-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
  }

  .build-card {
    position: relative;
    border-radius: 16px;
    background: var(--bg-surface);
    border: 1px solid var(--border-subtle);
    padding: 32px 28px;
    display: flex;
    flex-direction: column;
    transition: all 0.25s ease;
  }

  .build-card:hover {
    transform: translateY(-4px);
    border-color: var(--border-hover);
    box-shadow: 0 20px 48px -12px rgba(0, 0, 0, 0.7);
  }

  .build-featured {
    border-color: rgba(139, 108, 255, 0.45);
    background: linear-gradient(180deg, rgba(139, 108, 255, 0.08) 0%, var(--bg-surface) 35%);
  }

  .featured-ribbon {
    position: absolute;
    top: 14px;
    right: 18px;
    padding: 3px 10px;
    border-radius: 100px;
    font-size: 0.72rem;
    font-weight: 700;
    text-transform: uppercase;
    background: var(--accent-orange);
    color: #0c0e0f;
  }

  .build-card-header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    margin-bottom: 16px;
  }

  .build-variant-index {
    font-family: "Space Grotesk", sans-serif;
    font-size: 0.82rem;
    color: var(--text-muted);
    font-weight: 600;
  }

  .build-name {
    font-size: 1.6rem;
    color: var(--text-primary);
    margin-top: 2px;
  }

  .build-chip {
    padding: 4px 10px;
    border-radius: 6px;
    font-size: 0.74rem;
    font-weight: 700;
    text-transform: uppercase;
  }

  .chip-primary {
    background: var(--accent-orange-soft);
    color: var(--accent-orange);
    border: 1px solid rgba(189, 155, 255, 0.34);
  }

  .chip-neutral {
    background: rgba(255, 255, 255, 0.05);
    color: var(--text-secondary);
    border: 1px solid var(--border-medium);
  }

  .chip-accent {
    background: var(--accent-teal-soft);
    color: var(--accent-teal-bright);
    border: 1px solid rgba(139, 108, 255, 0.28);
  }

  .build-desc {
    font-size: 0.92rem;
    color: var(--text-secondary);
    line-height: 1.55;
    margin-bottom: 24px;
    min-height: 66px;
  }

  .build-feature-list {
    display: flex;
    flex-direction: column;
    gap: 12px;
    padding: 20px 0;
    border-top: 1px solid var(--border-subtle);
    border-bottom: 1px solid var(--border-subtle);
    margin-bottom: 24px;
    flex-grow: 1;
  }

  .build-feature-row {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 0.88rem;
    color: var(--text-secondary);
  }

  .check-icon {
    color: var(--accent-teal-bright);
    flex-shrink: 0;
  }

  .build-footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .build-tag-text {
    font-size: 0.78rem;
    color: var(--text-muted);
  }

  .build-link-btn {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 0.88rem;
    font-weight: 600;
    color: var(--accent-teal-bright);
  }

  .build-link-btn:hover {
    gap: 9px;
  }

  /* Final CTA Section */
  .final-cta-section {
    position: relative;
    padding: 120px 0;
    overflow: hidden;
    text-align: center;
  }

  .final-cta-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
    background:
      linear-gradient(120deg, rgba(139, 108, 255, 0.2), transparent 42%),
      linear-gradient(300deg, rgba(189, 155, 255, 0.14), transparent 38%),
      #0c0a14;
  }

  .final-cta-overlay {
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at center, rgba(12, 14, 15, 0.7) 0%, rgba(12, 14, 15, 0.95) 100%);
  }

  .final-cta-container {
    position: relative;
    z-index: 1;
    max-width: 740px;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .final-cta-icon-box {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 64px;
    height: 64px;
    border-radius: 50%;
    background: var(--accent-orange);
    color: #0c0e0f;
    box-shadow: 0 0 34px rgba(139, 108, 255, 0.52);
    margin-bottom: 28px;
  }

  .final-cta-title {
    font-size: clamp(2.4rem, 5.5vw, 4.2rem);
    line-height: 1.06;
    color: var(--text-primary);
    margin-bottom: 16px;
  }

  .final-cta-subtitle {
    font-size: 1.15rem;
    color: var(--text-secondary);
    line-height: 1.6;
    margin-bottom: 36px;
  }

  .final-cta-actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 16px;
  }

  .preview-channel {
    padding: 72px 0;
    border-top: 1px solid var(--border-subtle);
    background:
      radial-gradient(circle at 15% 20%, rgba(139, 108, 255, 0.18), transparent 38%),
      linear-gradient(135deg, var(--bg-surface-elevated), var(--bg-surface));
  }

  .preview-channel-inner {
    display: grid;
    grid-template-columns: minmax(0, 1.25fr) minmax(280px, 0.75fr);
    gap: clamp(40px, 8vw, 96px);
    align-items: center;
  }

  .preview-kicker {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 14px;
    color: var(--accent-teal-bright);
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .preview-copy h2 {
    max-width: 720px;
    margin-bottom: 14px;
    color: var(--text-primary);
    font-size: clamp(2rem, 4vw, 3.25rem);
    line-height: 1.08;
  }

  .preview-copy > p:not(.preview-kicker) {
    max-width: 720px;
    color: var(--text-secondary);
    font-size: 1rem;
  }

  .preview-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-top: 22px;
  }

  .preview-tags span {
    padding: 5px 10px;
    border: 1px solid var(--border-medium);
    border-radius: 100px;
    background: var(--accent-teal-soft);
    color: var(--text-secondary);
    font-size: 0.76rem;
    font-weight: 600;
  }

  .preview-actions {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 14px;
  }

  .preview-actions p {
    max-width: 440px;
    color: var(--text-muted);
    font-size: 0.84rem;
    line-height: 1.55;
  }

  /* Footer */
  .site-footer {
    border-top: 1px solid var(--border-subtle);
    background: var(--bg-surface);
    padding: 72px 0 36px;
  }

  .footer-container {
    display: grid;
    grid-template-columns: 1.3fr 2fr;
    gap: 64px;
    margin-bottom: 56px;
  }

  .footer-brand-col {
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .footer-tagline {
    font-size: 0.92rem;
    color: var(--text-muted);
    max-width: 340px;
    line-height: 1.6;
  }

  .footer-links-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
  }

  .footer-link-group {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .footer-link-group h5 {
    font-size: 0.85rem;
    font-weight: 700;
    color: var(--text-primary);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: 4px;
  }

  .footer-link-group a {
    font-size: 0.9rem;
    color: var(--text-secondary);
  }

  .footer-link-group a:hover {
    color: var(--accent-teal-bright);
  }

  .footer-bottom {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-top: 28px;
    border-top: 1px solid var(--border-subtle);
    font-size: 0.84rem;
    color: var(--text-muted);
  }

  .footer-bottom-links {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .footer-bottom-links a:hover {
    color: var(--text-secondary);
  }

  .footer-dot {
    opacity: 0.4;
  }

  :root[data-theme="light"] .site-header {
    background: rgba(247, 245, 255, 0.88);
    box-shadow: 0 1px 24px rgba(54, 38, 92, 0.08);
  }

  :root[data-theme="light"] .inline-code,
  :root[data-theme="light"] .nav-icon-btn,
  :root[data-theme="light"] .theme-toggle,
  :root[data-theme="light"] .btn-secondary,
  :root[data-theme="light"] .card-icon-box,
  :root[data-theme="light"] .card-tag,
  :root[data-theme="light"] .chip-neutral {
    background: rgba(101, 71, 215, 0.07);
  }

  :root[data-theme="light"] .nav-icon-btn:hover,
  :root[data-theme="light"] .btn-secondary:hover,
  :root[data-theme="light"] .btn-ghost:hover,
  :root[data-theme="light"] .pillar-box:hover {
    background: rgba(101, 71, 215, 0.12);
  }

  :root[data-theme="light"] .mockup-frame,
  :root[data-theme="light"] .showcase-tile,
  :root[data-theme="light"] .bento-card,
  :root[data-theme="light"] .build-card {
    box-shadow: 0 18px 44px -16px rgba(54, 38, 92, 0.22);
  }

  :root[data-theme="light"] .mockup-bar {
    background: rgba(247, 245, 255, 0.92);
  }

  :root[data-theme="light"] .mockup-dots span {
    box-shadow: inset 0 0 0 1px rgba(24, 18, 37, 0.1);
  }

  :root[data-theme="light"] .philosophy-card {
    background: linear-gradient(135deg, #ffffff 0%, #eee9ff 100%);
    box-shadow: 0 24px 60px rgba(54, 38, 92, 0.15);
  }

  :root[data-theme="light"] .pillar-box {
    background: rgba(101, 71, 215, 0.05);
  }

  :root[data-theme="light"] .final-cta-title {
    color: #f7f4ff;
  }

  :root[data-theme="light"] .final-cta-subtitle {
    color: #c9c1d8;
  }

  :root[data-theme="light"] .final-cta-section .btn-secondary {
    background: rgba(255, 255, 255, 0.1);
    color: #f7f4ff;
    border-color: rgba(255, 255, 255, 0.22);
  }

  :root[data-theme="light"] .final-cta-section .btn-secondary:hover {
    background: rgba(255, 255, 255, 0.16);
  }

  /* Responsive Design */
  @media (max-width: 980px) {
    .bento-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .stat-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .philosophy-grid {
      grid-template-columns: 1fr;
    }

    .builds-grid {
      grid-template-columns: 1fr;
    }

    .footer-container {
      grid-template-columns: 1fr;
      gap: 40px;
    }

    .preview-channel-inner {
      grid-template-columns: 1fr;
      gap: 30px;
    }
  }

  @media (max-width: 768px) {
    .nav-links {
      display: none;
    }

    .github-label {
      display: none;
    }

    .version-badge {
      display: none;
    }

    .header-inner {
      padding-inline: 16px;
    }

    .header-cta {
      width: 38px;
      height: 38px;
      padding: 0;
      justify-content: center;
    }

    .header-cta span {
      display: none;
    }

    .bento-grid {
      grid-template-columns: 1fr;
    }

    .showcase-bento {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      grid-auto-rows: auto;
    }

    .showcase-tile {
      grid-column: span 1;
      grid-row: auto;
    }

    .showcase-tile--controls {
      grid-column: 1 / -1;
    }

    .showcase-tile--landscape {
      aspect-ratio: 20 / 9;
    }

    .showcase-tile--portrait {
      aspect-ratio: 9 / 20;
    }

    .mockup-overlay-badge {
      display: none;
    }

    .stat-grid {
      grid-template-columns: 1fr;
    }

    .footer-links-grid {
      grid-template-columns: 1fr;
      gap: 28px;
    }

    .footer-bottom {
      flex-direction: column;
      gap: 14px;
      text-align: center;
    }
  }

  @media (max-width: 480px) {
    .showcase-bento {
      grid-template-columns: 1fr;
      gap: 14px;
    }

    .showcase-tile,
    .showcase-tile--controls {
      grid-column: 1;
    }

    .hero-btn-row {
      flex-direction: column;
      width: 100%;
    }

    .btn {
      width: 100%;
    }

    .final-cta-actions {
      flex-direction: column;
      width: 100%;
    }
  }
</style>
