{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "git": {
    "deploymentEnabled": {
      "master": true,
      "*": false
    }
  },
  "ignoreCommand": "if [ \"$VERCEL_ENV\" = \"production\" ] && [ -z \"$VERCEL_GIT_PULL_REQUEST_ID\" ]; then exit 1; else exit 0; fi",
  "installCommand": "test -f website/package-lock.json || { echo 'Vercel Root Directory must be the repository root (.), not website.' >&2; exit 1; }; npm --prefix website ci",
  "buildCommand": "npm --prefix website run build",
  "outputDirectory": "website/dist",
  "framework": null
}
